[workspace]
resolver = "2"
members = ["protocol", "clocksync", "mic2sock"]

# 排除 mic2sock：它需要 libjack，在无 libjack 的机器（含 CI 容器）上
# 无法构建。这样 `cargo build` / `cargo test` 默认只处理纯逻辑 crate。
# Pi 上构建守护进程用：cargo build -p mic2sock --release
default-members = ["protocol", "clocksync"]
